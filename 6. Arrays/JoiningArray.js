
//Joining array element using join method/function
const numbers=[1,2,3,4,5];
const joined= numbers.join(',');
console.log(joined);

// saprate string into array using split method
const message="This is my first message";
const parts=message.split(' ');
console.log(parts);

// combine string split string array with join method
const combined=parts.join('-');
console.log(combined);